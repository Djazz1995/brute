import { ScreenPlaceholder } from '@/components/screen-placeholder';

export function SettingsScreen() {
  return (
    <ScreenPlaceholder
      title="Settings"
      section="§7.2"
      note="Default rudeness, escalation, quiet hours, sound, sharing prefs."
    />
  );
}
