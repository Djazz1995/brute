import { ScreenPlaceholder } from '@/components/screen-placeholder';

export function OnboardingScreen() {
  return (
    <ScreenPlaceholder
      title="Onboarding"
      section="§14.1"
      note="Welcome → harsh-humor consent → defaults → push permission → first goal."
    />
  );
}
