import { ScreenPlaceholder } from '@/components/screen-placeholder';

export function HomeScreen() {
  return (
    <ScreenPlaceholder
      title="Home"
      section="§4.7"
      note="All goals at a glance: done / pending / skipped today + streak summary."
    />
  );
}
